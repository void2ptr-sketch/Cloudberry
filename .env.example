# Copy to `.env` for local overrides. The `.env` file is gitignored.
# Do not put real tokens, passwords, or API keys in the repository.

# REST API base URL (future backend / local tooling)
API_URL=http://localhost:3000/api

# Optional bearer token for manual API testing (runtime: AuthTokenService / localStorage)
# AUTH_TOKEN=
