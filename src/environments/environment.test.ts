import type { Environment } from './environment.model';

export const environment: Environment = {
  production: false,
  name: 'test',
  apiUrl: 'https://api.test.cloudberry.example/api',
  enableDebug: true,
};
