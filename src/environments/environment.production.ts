import type { Environment } from './environment.model';

export const environment: Environment = {
  production: true,
  name: 'prod',
  apiUrl: 'https://api.cloudberry.example/api',
  enableDebug: false,
};
